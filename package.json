{
  "name": "findmysensi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "tools/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @findmysensi/web",
    "format:check": "prettier --check .",
    "format": "prettier --write .",
    "lint": "eslint .",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -b && npm run build --workspace @findmysensi/web",
    "clean": "npm run clean --workspaces --if-present",
    "check:boundaries": "node tools/architecture/check-boundaries.mjs",
    "decisions:check": "tsx tools/decisions/validator.ts",
    "protocol:freeze:check": "tsx tools/protocol/freeze-validator.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "fast-check": "^3.23.2",
    "prettier": "^3.5.1",
    "tsx": "4.23.13",
    "typescript": "^7.0.2",
    "typescript-eslint": "^8.69.0",
    "vitest": "^5.0.0",
    "zod": "4.5.4"
  }
}
